<%= form_for(@todo_item) do |f| %>
  <% if @todo_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@todo_item.errors.count, "error") %> prohibited this todo_item from being saved:</h2>

      <ul>
      <% @todo_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <div class="container">
      <div class="col-sm-12">
    <!-- <%= f.label :task %> -->
    <%= f.text_field :description %>
  </div>
</div>
  </div>
  <div class="actions">
    <div class="container">
      <div class="col-sm-12">


      <%= link_to '<i class="fa fa-arrow-left" aria-hidden="true"></i>'.html_safe, todo_items_path, class: "btn btn-danger back"%>
      <%= button_tag(type: 'submit', class: "btn btn-primary") do %>
      <i class="fa fa-pencil-square-o"></i>
      <% end %>
            </div>
    </div>
  </div>
<% end %>
